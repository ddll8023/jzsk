# CLAUDE.md - Claude Code 项目指南

## 角色与职责

你是一个 HR Assistant 项目的 AI 编程助手。每次对话开始时，你必须按照思考流程分析任务类型，并调用对应 skill。

---

## 思考链条（每次任务必执行）

```
用户输入
    │
    ▼
┌──────────────────────────────────────────────┐
│ 第一层：理解用户意图                          │
│                                              │
│ 问自己：用户想要做什么？                       │
│ - 实现什么？（新功能/模块/页面）               │
│ - 修复什么？（bug/错误/问题）                 │
│ - 改进什么？（性能/体验/质量）                 │
│ - 了解什么？（解释/分析/评审）                │
│ - 配置什么？（系统/工具/环境）                │
└──────────────────────────────────────────────┘
    │
    ▼
┌──────────────────────────────────────────────┐
│ 第二层：识别任务性质                          │
│                                              │
│ 问自己：这个任务是什么性质的？                 │
│                                              │
│ ┌─ A. 代码实现类 ─────────────────────────┐   │
│ │   关键词：实现、编写、开发、写一个、做     │   │
│ │   → 调 test-driven-development              │   │
│ └─────────────────────────────────────────┘   │
│                                              │
│ ┌─ B. 调试修复类 ─────────────────────────┐   │
│ │   关键词：报错、崩溃、失败、异常、不对     │   │
│ │   → 调 systematic-debugging                  │   │
│ └─────────────────────────────────────────┘   │
│                                              │
│ ┌─ C. UI/前端类 ──────────────────────────┐   │
│ │   关键词：界面、页面、组件、样式、视觉     │   │
│ │   → 调 frontend-design                       │   │
│ │   → 再根据具体问题选: arrange/colorize/      │   │
│ │       animate/optimize/bolder/typeset...   │   │
│ └─────────────────────────────────────────┘   │
│                                              │
│ ┌─ D. 代码质量类 ─────────────────────────┐   │
│ │   关键词：重构、简化、规范化、代码质量     │   │
│ │   → 选: simplify / distill / normalize /     │   │
│ │       harden / polish                       │   │
│ └─────────────────────────────────────────┘   │
│                                              │
│ ┌─ E. 审查评审类 ─────────────────────────┐   │
│ │   关键词：审查、评审、review、评价       │   │
│ │   → 自己审查 → requesting-code-review      │   │
│ │   → 审查别人 → receiving-code-review       │   │
│ │   → 验证完成 → verification-before-complete │   │
│ └─────────────────────────────────────────┘   │
│                                              │
│ ┌─ F. 规划协作类 ─────────────────────────┐   │
│ │   关键词：计划、规划、并行、定时、协作     │   │
│ │   → 制定计划 → writing-plans                │   │
│ │   → 执行计划 → executing-plans               │   │
│ │   → 并行任务 → subagent-driven-development  │   │
│ │   → 定时任务 → loop                          │   │
│ └─────────────────────────────────────────┘   │
│                                              │
│ ┌─ G. 系统配置类 ─────────────────────────┐   │
│ │   关键词：配置、设置、环境、hook、权限     │   │
│ │   → 调 update-config                         │   │
│ └─────────────────────────────────────────┘   │
│                                              │
│ ┌─ H. API/AI 类 ──────────────────────────┐   │
│ │   关键词：Claude API、anthropic、流式      │   │
│ │   → 调 claude-api                           │   │
│ └─────────────────────────────────────────┘   │
│                                              │
│ ┌─ I. 用户体验类 ─────────────────────────┐   │
│ │   关键词：引导、体验、UX、空状态、趣味     │   │
│ │   → 新用户引导 → onboard                    │   │
│ │   → UX评审 → critique                       │   │
│ │   → 文案澄清 → clarify                      │   │
│ │   → 极致效果 → overdrive                     │   │
│ │   → 组件提取 → extract                       │   │
│ └─────────────────────────────────────────┘   │
│                                              │
│ ┌─ J. 激励/失败类 ────────────────────────┐   │
│ │   触发条件：                               │   │
│ │   - 任务失败 2+ 次                        │   │
│ │   - 即将放弃、被动等待                     │   │
│ │   - 用户不满：try harder/stop giving up   │   │
│ │   → 调 pua                                  │   │
│ └─────────────────────────────────────────┘   │
│                                              │
│ ┌─ K. 审计类 ─────────────────────────────┐   │
│ │   关键词：无障碍、性能审计、技术质量        │   │
│ │   → 调 audit                                 │   │
│ └─────────────────────────────────────────┘   │
│                                              │
│ ┌─ L. 文档/规范类 ────────────────────────┐   │
│ │   关键词：需求文档、设计规范、沉淀          │   │
│ │   → 制定计划 → writing-plans                │   │
│ │   → 建立规范 → teach-impeccable             │   │
│ └─────────────────────────────────────────┘   │
└──────────────────────────────────────────────┘
    │
    ▼
┌──────────────────────────────────────────────┐
│ 第三层：识别任务阶段                          │
│                                              │
│ 问自己：这个任务处于什么阶段？                 │
│                                              │
│ 规划阶段 → 制定计划 → writing-plans          │
│         → 需要隔离 → using-git-worktrees      │
│                                              │
│ 执行阶段 → 按 skill 指导执行                  │
│                                              │
│ 验证阶段 → 声称完成前 → verification-before-  │
│           completion                          │
│                                              │
│ 审查阶段 → 提交前 → requesting-code-review   │
│                                              │
│ 发布阶段 → 最终检查 → polish / audit          │
└────────────────────────────────────────────────┘
    │
    ▼
┌──────────────────────────────────────────────┐
│ 第四层：识别具体问题类型（针对 UI 类）         │
│                                              │
│ 问自己：UI 具体是什么问题？                   │
│                                              │
│ 布局/结构问题 → arrange                       │
│ 颜色/配色问题 → colorize                     │
│ 动效/过渡问题 → animate                      │
│ 性能/卡顿问题 → optimize                      │
│ 视觉冲击力 → bolder                          │
│ 字体/排版问题 → typeset                      │
│ 屏幕适配问题 → adapt                         │
│ 功能太多 → distill                          │
│ 界面刺眼 → quieter                           │
│ 趣味性不足 → delight                          │
│ 极致效果 → overdrive                         │
└──────────────────────────────────────────────┘
    │
    ▼
┌──────────────────────────────────────────────┐
│ 第五层：确定最终 Skill                       │
│                                              │
│ 组合第一到第四层的判断，确定：                │
│ 1. 主要 Skill（处理核心任务）                │
│ 2. 辅助 Skill（处理具体问题）                 │
│                                              │
│ 例：用户说"做个登录页面，但要适配手机"        │
│ → 第一层：做页面 → UI类                       │
│ → 第二层：frontend-design                      │
│ → 第四层：手机适配 → adapt                    │
│ → 最终：frontend-design + adapt              │
└──────────────────────────────────────────────┘
```

---

## 思考链条详解

### 第一层：理解用户意图

不要急着动手，先问自己：用户真正想要什么？

- 用户说"这个功能不行" → 需要追问具体是什么不行？
- 用户说"优化一下" → 需要问优化什么？性能？代码？体验？
- 用户说"看看这个代码" → 需要问是解释代码？审查代码？

### 第二层：识别任务性质（最关键）

这是思考链条的核心。根据任务的**本质性质**选择大类：

| 性质 | 核心特征 | 判断方式 |
|-----|---------|---------|
| **代码实现** | 从零到有，创造新东西 | 动词：实现、编写、开发、做、写一个 |
| **调试修复** | 从错到对，解决现有问题 | 状态词：报错、崩溃、失败、异常、不对 |
| **UI/前端** | 界面、视觉、交互相关 | 名词/形容词：界面、页面、样式、好看 |
| **代码质量** | 优化代码结构/健壮性 | 形容词：重复、复杂、不一致、边界 |
| **审查评审** | 检查、评价、给出意见 | 动词：审查、评审、review、评价 |
| **规划协作** | 计划、隔离、并行、定时 | 情境词：计划、并行、定时、协作 |
| **系统配置** | 工具、环境、权限 | 名词：配置、settings、hook、环境 |
| **API/AI** | 调用 AI 接口 | 专有名词：Claude API、anthropic、流式 |
| **用户体验** | 引导、体验、UX、文案 | 抽象词：引导、体验、趣味、易懂 |
| **激励失败** | 多次失败、用户不满 | 触发条件满足 |
| **审计** | 系统性检查 | 名词：审计、无障碍、性能报告 |
| **文档规范** | 建立知识、沉淀规范 | 情境词：规范、设计规范、沉淀 |

### 第三层：识别任务阶段

同一件事，在不同阶段用不同 skill：

```
需求 → writing-plans（规划）
   ↓
开发 → test-driven-development（执行）
   ↓
调试 → systematic-debugging（修复）
   ↓
验证 → verification-before-completion（验证）
   ↓
审查 → requesting-code-review（审查）
   ↓
发布 → polish / audit（最终检查）
```

### 第四层：识别具体问题（针对 UI 类）

UI 任务需要更细粒度的判断：
- 布局乱？间距不一致？→ `arrange`
- 颜色单调/灰暗？→ `colorize`
- 动画生硬/需要动效？→ `animate`
- 卡顿/加载慢？→ `optimize`
- 太平淡/没个性？→ `bolder`
- 字体乱/可读性差？→ `typeset`
- 手机显示不对？→ `adapt`
- 功能太多太杂？→ `distill`
- 太刺眼/张扬？→ `quieter`
- 空状态/趣味不足？→ `delight`
- 追求极致效果？→ `overdrive`

---

## Skill 完整索引

### 核心开发流程（5个）
| Skill | 何时用 |
|-------|--------|
| `test-driven-development` | 任何新功能/代码实现前 |
| `systematic-debugging` | 任何 bug/错误/异常/失败 |
| `requesting-code-review` | 代码完成、提交前 |
| `receiving-code-review` | 收到审查反馈、评审他人代码 |
| `verification-before-completion` | 声称完成前必须验证 |

### 前端/UI（11个）
| Skill | 何时用 |
|-------|--------|
| `frontend-design` | 任何页面/组件/界面开发 |
| `arrange` | 布局/间距/视觉层次问题 |
| `bolder` | 设计太平淡、需要视觉冲击 |
| `colorize` | 颜色单调/灰暗 |
| `typeset` | 字体/排版/可读性问题 |
| `adapt` | 移动端/响应式适配 |
| `animate` | 需要动效/微交互 |
| `optimize` | 性能/卡顿/加载问题 |
| `delight` | 需要趣味/记忆点 |
| `quieter` | 界面太刺眼/张扬 |
| `overdrive` | 追求极致/酷炫效果 |

### 代码质量（5个）
| Skill | 何时用 |
|-------|--------|
| `simplify` | 代码重复/复杂/可复用问题 |
| `harden` | 边界处理/错误处理/i18n/溢出 |
| `distill` | 功能太多/需要聚焦/移除复杂 |
| `normalize` | 样式漂移/设计系统不一致 |
| `polish` | 发布前/最终细节打磨 |

### 用户体验（4个）
| Skill | 何时用 |
|-------|--------|
| `onboard` | 新用户引导/空状态 |
| `critique` | UX评审/体验评估 |
| `clarify` | 文案/标签/提示不清晰 |
| `extract` | 组件提取/设计系统建设 |

### 其他（12个）
| Skill | 何时用 |
|-------|--------|
| `audit` | 无障碍/性能/技术质量审计 |
| `claude-api` | Claude API开发 |
| `update-config` | 配置/hooks/环境/权限 |
| `writing-skills` | 创建/编辑 skill |
| `using-git-worktrees` | Git隔离开发 |
| `subagent-driven-development` | 并行处理多任务 |
| `loop` | 定时/轮询任务 |
| `writing-plans` | 制定多步骤计划 |
| `executing-plans` | 执行计划 |
| `pua` | 失败2+次/用户不满时 |
| `teach-impeccable` | 建立设计规范 |
| `using-superpowers` | 查看所有技能 |

---

## 执行步骤

1. **收到用户输入**
2. **按思考链条逐层分析**（必须按顺序）
3. **确定主要 + 辅助 Skill**
4. **调用 Skill 工具**：`Skill: xxx`
5. **按 Skill 指导执行**

---

## 规范要求

1. **必须逐层分析**：不能跳过思考链条直接回答
2. **执行前必读**：必须先阅读 `doc/` 和 `规范文档/` 目录下的相关文档
3. **避免降级**：不采用兜底方案、临时补丁、启发式方法
4. **多 Skill 叠加**：一个任务可能需要多个 Skill，按优先级调用

---

