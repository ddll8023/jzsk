# CLAUDE.md - Claude Code 项目指南

**YOU MUST** 按照以下流程执行所有任务。

> **工作流定义：** 详细工作流规范见 [规范文档/编程工作流.md](./规范文档/编程工作流.md)

---

## 思考与执行流程

```
用户输入 → 任务分类 → 思考链执行 → 方案展示 → 等待"嘻嘻"
                                                    ↓
                                              收到"嘻嘻"
                                                    ↓
                                             代码执行
                                                    ↓
                                         GAN 对抗评估
                                           ↙       ↘
                                     ✅ 通过    ❌ 不通过
                                        ↓          ↓
                                      完成      返回修复
```

### ⚠️ 强制要求：必须主动读取，不能依赖摘要

**每次任务开始前，必须用 Read 工具读取以下文件（完整读取，不是摘要）：**

| 文件 | 路径 |
|------|------|
| 项目 CLAUDE.md | `D:\demo\java\jzsk\CLAUDE.md` |
| 编程工作流 | `D:\demo\java\jzsk\规范文档\编程工作流.md` |
| 后端规范文档 | `D:\demo\java\jzsk\规范文档\后端规范文档.md` |

**禁止行为：**
- ❌ 只看系统提示中的摘要就认为"已了解"
- ❌ 因为任务"简单"就跳过规范流程
- ❌ 不使用 Read 工具，直接分析问题

**正确做法：**
- ✅ 用 Read 工具读取完整文件
- ✅ 即使是调试问答，也要遵循思考链
- ✅ 所有任务都需要展示方案，等待"嘻嘻"

### GAN 对抗评估（三层评估）

代码修改完成后必须进行 GAN 对抗评估，详见 [规范文档/编程工作流.md](./规范文档/编程工作流.md#五gan-对抗评估)

| 评估层 | 内容                                          |
| ------ | --------------------------------------------- |
| 第一层 | 问题分析评估 - 问题理解是否正确               |
| 第二层 | 方案完整性评估 ⚠️ - 相关文件是否都考虑到了  |
| 第三层 | 代码质量评估 - 简洁性、规范性、健壮性、可读性 |

---

## 任务分类

| 类型     | 标识词           | 对应 Skill               |
| -------- | ---------------- | ------------------------ |
| 代码实现 | 添加、实现、新增 | test-driven-development  |
| 调试修复 | 修复、bug、错误  | systematic-debugging     |
| 前端 UI  | 界面、页面、样式 | frontend-design + 子技能 |
| 代码质量 | 优化、重构、简化 | simplify/harden/distill  |
| 审查评审 | 审查、review     | requesting-code-review   |
| 系统配置 | 配置、环境       | update-config            |
| 用户体验 | 体验、引导       | onboard/critique         |
| 技术审计 | 审计、性能       | audit                    |

---

## 阶段自动识别

| 用户输入         | 识别结果           | 下一步         |
| ---------------- | ------------------ | -------------- |
| 展示方案后       | 等待确认           | 等待"嘻嘻"     |
| "好的/可以/同意" | 确认方案           | 继续等待"嘻嘻" |
| "嘻嘻"           | **执行代码** | 进入代码执行   |
| 提出修改意见     | 新方案设计         | 返回阶段二     |

---

## 思考链（所有任务都必须完整执行，包括问答）

```
第零层：⚠️ 必须用 Read 工具读取 CLAUDE.md 和规范文档（不是摘要，是完整读取）
  ↓
第一层：理解用户意图
  ↓
第二层：识别任务性质（代码实现/调试修复/UI前端/代码质量/审查评审/系统配置/用户体验/审计/文档规范）
  ↓
第三层：识别任务阶段（需求→writing-plans，方案确认→展示方案，开发→test-driven-development，验证→verification-before-completion，审查/发布→requesting-code-review）
  ↓
第四层：识别具体问题类型（针对UI）布局→arrange，颜色→colorize，动画→animate，性能→optimize，简化→simplify
  ↓
第五层：确定最终 Skill（可多个叠加）
  ↓
第六层：确认方案（Skill 调用后才能展示）
```

**⚠️ 严禁在调用 Skill 之前展示方案**

---

## 强制检查清单

执行代码前必须验证：

- [ ] 已读取 doc/ 和 规范文档/ 目录相关文档
- [ ] 已调用 Skill
- [ ] 已展示方案
- [ ] 用户已给出"嘻嘻"（不含"嘻嘻"一律不执行代码）

**⚠️ 每次代码修改都需独立确认"嘻嘻"**

---

## 方案确认模板

> 详细模板见 [规范文档/编程工作流.md](./规范文档/编程工作流.md#63-方案确认模板)

```markdown
─────────────────────────────────────────
📋 **方案确认**

**🔍 思考链：**
- 第零层：是否已读取 doc/ 和 规范文档/？✅/❌
- 第一层：用户意图 → ...
- 第二层：任务性质 → ...
- 第三层：任务阶段 → ...
- 第四层：具体问题 → ...
- 第五层：选择 Skill → 主要: xxx，辅助: xxx（如有）

**✅ Skill 状态：** [x] 已调用 xxx

---

**📌 任务理解：**
- **目标**：...
- **范围**：...

---

**📝 实现步骤：**
1. ...
2. ...
3. ...

---

**📁 涉及文件：**
| 文件 | 操作 | 说明 |
|------|------|------|
| `xxx.java` | 修改 | ... |
| `yyy.java` | 新增 | ... |

---

─────────────────────────────────────────
⏸️ **方案已展示**
─────────────────────────────────────────
```

---

## 规范要求

1. **必须逐层分析** - 不能跳过思考链
2. **执行前必读** - 先读 doc/ 和 规范文档/
3. **避免降级** - 不用兜底方案、临时补丁
4. **Skill 顺序** - 思考链 → 调用 Skill → 展示方案 → 等待嘻嘻
5. **强制安全词** - 只有"嘻嘻"触发代码执行
6. **方案先确认** - 展示方案后再执行
7. **GAN 评估** - 代码修改后必须进行三层评估

---

## 相关文档

- [规范文档/编程工作流.md](./规范文档/编程工作流.md) - 完整工作流定义
- [规范文档/CLAUDE.md编写规范.md](./规范文档/CLAUDE.md编写规范.md) - CLAUDE.md 编写规范
