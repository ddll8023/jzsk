<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.szy.mapper.IndividualPressureSitesMapper">

    <insert id="add">
        insert into individual_pressure_sites(name, station_number, rtu_code, measuring_stations_elements,
                                              split_site_code, address, longitude, latitude, up_up_limit, up_limit,
                                              low_limit, lower_limit, note, create_time, update_time, point)
        values(#{name},#{stationNumber},#{rtuCode},#{measuringStationsElements},#{splitSiteCode},#{address},
               #{longitude},#{latitude},#{upUpLimit},#{upLimit},#{lowLimit},#{lowerLimit},#{note},
               #{createTime},#{updateTime},st_geometryfromtext(#{geom}))
    </insert>
    <update id="addPosition">
        update individual_pressure_sites
        set point = st_geometryfromtext(#{geom})
        where id = #{id}
    </update>
</mapper>
