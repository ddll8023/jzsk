<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.szy.mapper.UserMapper">
    <select id="getAuthorityIDs" resultType="java.lang.Long">
        SELECT
            DISTINCT ra.authority_id
        FROM
            user_role ur
                LEFT JOIN role_authority ra ON ur.role_id = ra.role_id
        WHERE ur.user_id = #{userId}
    </select>
    <select id="listByAuthorityID" resultType="com.szy.entity.User">
        SELECT
            DISTINCT u.*
        FROM
            user_role ur
                LEFT JOIN role_authority ra ON ur.role_id = ra.role_id
                LEFT JOIN user u ON ur.user_id = u.id
        WHERE
            ra.authority_id = #{authority_id}
    </select>
</mapper>
