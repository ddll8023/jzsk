<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.szy.mapper.WarningIndicatorSettingMapper">
    <select id="getByPosition" resultType="com.szy.entity.WarningIndicatorSetting">
        select id,position,type,up_up_limit,up_limit,low_limit,lower_limit,unit,longitude,latitude,create_time,update_time
        from warning_indicator_setting
        where position = #{position}
        and type = #{type}
    </select>
    <select id="getAllTypes" resultType="java.lang.String">
        select distinct(type) from warning_indicator_setting
    </select>
</mapper>
