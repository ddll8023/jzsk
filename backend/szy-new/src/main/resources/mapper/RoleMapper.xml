<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.szy.mapper.RoleMapper">

    <resultMap id="BaseResultMap" type="com.szy.pojo.entity.Role">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="code" property="code"/>
        <result column="note" property="note"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, code, note, type, status, create_time, update_time
    </sql>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM role
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
        ORDER BY id
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM role
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.szy.pojo.entity.Role" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO role (name, code, note, type, status, create_time, update_time)
        VALUES (#{name}, #{code}, #{note}, #{type}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.szy.pojo.entity.Role">
        UPDATE role
        SET name = #{name},
            code = #{code},
            note = #{note},
            type = #{type},
            status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM role WHERE id = #{id}
    </delete>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT r.*
        FROM role r
        LEFT JOIN user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

</mapper>